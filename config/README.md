# Configuration

This directory is reserved for CAD joint and actuator calibration data. The CAD
integration will record each moving component's node name, axis, home offset,
movement direction, travel limit, shaft zero angle, and any screw lead or gear
ratio here rather than hard-coding those values in the viewer.
